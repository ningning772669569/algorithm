#include <cstdio>
#include <iostream>
#include <bits/stdc++.h>
using namespace std;
const int maxn=1000;
int dp[maxn][maxn];
int main ()
{
    sting a,b;
    cin >> a>>b;
    memset(dp,0,sizeof(dp));

    return 0;
}
